// This is a sample model to get you started.

datasource db {
    provider = 'sqlite'
}

type Profile {
    age    Int
    gender String?
}

model User {
    id       Int    @id @default(autoincrement())
    email    String @unique
    profile  Profile @json
}
